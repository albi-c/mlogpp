const LOOP_UNTIL = 5

num x = 10

function func1(num x, num y) -> num {
    x = 12
    y = 20
    return x + y
}

function func2(num n) {
    x = n ** 2
}

num val = func1(3, 4)
func2(val)

num y = 0
for (i : LOOP_UNTIL) {
    y += i * x
}

Block message1
print(x)
print(" ")
print(y)
printflush(message1)
